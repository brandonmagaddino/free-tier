# AWS Free Tier Reference

**Status: not yet covered.**

This skill is Azure-first today. AWS-specific free-tier guidance (Amplify Hosting, DynamoDB
free tier, Lambda's monthly free grant, the 12-month vs. always-free split AWS uses, etc.)
hasn't been written yet.

If the user is targeting AWS, say so plainly rather than improvising specifics from
general knowledge — general knowledge on free-tier limits goes stale fast and this file
hasn't been verified the way `azure.md` has. Give best-effort direction (AWS's shape is
broadly similar: static hosting + managed NoSQL + serverless functions), but don't quote
exact numbers you haven't confirmed against AWS's current pricing pages.

Contributions welcome — see the README's "Adding a new cloud" section for the expected
shape of this file (decision-tree style entries with a "Last verified" date, matching
`azure.md`).
